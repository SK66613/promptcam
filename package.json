{
  "name": "promptcam-web",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "check": "node --check web/app.js && node --check web/telegram.js && node --check worker/index.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
